const MESSAGE = {
  REQUIRED: 'This field is required',
  INVALID_EMAIL: 'Invalid email',
  MIN_PASSWORD: 'Password must be at least 10 characters',
  PASSWORDS_MUST_MATCH: 'Passwords must match',
} as const;

export default MESSAGE;